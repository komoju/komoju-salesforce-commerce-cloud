<isloop items="${pdict.order.billing.payment.applicablePaymentMethods}" var="paymentOption">
    <isif condition="${paymentOption.ID === 'komoju'}">
        <isinclude template="checkout/billing/paymentOptions/komojuContent" />
    </isif>
</isloop>
